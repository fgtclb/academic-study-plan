# Aggregate page TSconfig: every component this extension ships.
#
# The counterpart of the set "fgtclb/academic-study-plan" for installations that
# include page TSconfig through the page field "Page TSconfig" instead of
# through a site set.
@import 'EXT:academic_study_plan/Configuration/TSconfig/ContentElement/page.tsconfig'
