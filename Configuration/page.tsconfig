# Page TSconfig of this extension, auto-included for the whole installation.
#
# TYPO3 reads "Configuration/page.tsconfig" of every active extension since
# v12.0 (Feature: #96614), on TYPO3 v12 and v13 alike. It is loaded on every
# site, whatever the site configuration says, which makes it the right place for
# exactly one thing: the content element of this extension is hidden here, and
# re-enabled by the page TSconfig the component set delivers.
#
# The order TYPO3 merges page TSconfig in is package files first, then set
# files, then the site, then the page field "Page TSconfig", then the page
# record - so both ways to re-enable the content element come after this file
# and win over it. On TYPO3 v12 there is no set layer, and the page field is the
# only way.
TCEFORM.tt_content.CType.removeItems := addToList(academic_study_plan)
